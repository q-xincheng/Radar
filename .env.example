# ==============================================================================
# Industry Radar Environment Configuration Example
# ==============================================================================
# 
# Instructions:
# 1. Copy this file to .env (which is .gitignored)
# 2. Replace the placeholder values with your actual credentials
# 3. NEVER commit .env file with real credentials to version control
#
# ==============================================================================

# ------------------------------------------------------------------------------
# AI Provider Configuration (SiliconFlow)
# ------------------------------------------------------------------------------
# Required: API key for SiliconFlow AI service
# Get your key from: https://siliconflow.cn
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# ------------------------------------------------------------------------------
# Alibaba Cloud Credentials
# ------------------------------------------------------------------------------
# Required if using Alibaba Cloud Function Compute or OSS
# These should be from a RAM sub-account with minimal required permissions
# Get credentials from: RAM Console > Users > Create AccessKey
ALIBABA_CLOUD_ACCESS_KEY_ID=your_access_key_id_here
ALIBABA_CLOUD_ACCESS_KEY_SECRET=your_access_key_secret_here

# Optional: Account ID (if needed for specific operations)
# ALIBABA_CLOUD_ACCOUNT_ID=your_account_id_here

# ------------------------------------------------------------------------------
# LLM Configuration (Optional - defaults will be used if not set)
# ------------------------------------------------------------------------------
# LLM model to use
# LLM_MODEL=deepseek-ai/DeepSeek-V3

# LLM API base URL
# LLM_BASE_URL=https://api.siliconflow.cn/v1

# Maximum retries for LLM API calls
# LLM_MAX_RETRIES=3

# ------------------------------------------------------------------------------
# Storage Configuration (Optional)
# ------------------------------------------------------------------------------
# Local data storage directory
# DATA_DIR=data

# Storage backend: 'local' (default) or 'oss'
# STORAGE_BACKEND=local

# ------------------------------------------------------------------------------
# OSS Configuration (Required when STORAGE_BACKEND=oss)
# ------------------------------------------------------------------------------
# Alibaba Cloud OSS endpoint (e.g., oss-cn-hangzhou.aliyuncs.com)
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com

# OSS bucket name
# OSS_BUCKET=your-bucket-name

# OSS object key prefix (default: "radar/")
# OSS_PREFIX=radar/

# ------------------------------------------------------------------------------
# Database Configuration (Optional)
# ------------------------------------------------------------------------------
# SQLite database file path (default: ${DATA_DIR}/radar.db)
# DB_PATH=data/radar.db
# For FC environment, you might want to use /tmp:
# DB_PATH=/tmp/radar.db

# ------------------------------------------------------------------------------
# Logging Configuration (Optional)
# ------------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# ------------------------------------------------------------------------------
# Alert Notification Configuration (Optional)
# ------------------------------------------------------------------------------
# Alert notifications for scraping failures and critical errors

# DingTalk Robot Alert
# Get webhook URL from: DingTalk Group > Group Settings > Group Robot > Add Robot
# DINGTALK_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=your_token_here
# DINGTALK_SECRET=  # Optional, currently not used (signature not implemented)

# QQ Email Alert via SMTP
# Note: Use QQ Mail authorization code, not your QQ password
# Get authorization code from: QQ Mail Settings > Account > POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV Service
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=465  # Use 465 for SSL (recommended) or 587 for STARTTLS
# SMTP_USERNAME=your_email@qq.com
# SMTP_PASSWORD=your_qq_mail_authorization_code_here  # 16-character authorization code
# SMTP_TO=recipient1@example.com,recipient2@example.com  # Comma-separated for multiple recipients
# SMTP_FROM=your_email@qq.com  # Optional, defaults to SMTP_USERNAME
